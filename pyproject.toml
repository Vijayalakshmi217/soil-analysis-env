[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "soil-analysis-env"
version = "0.1.0"
description = "Soil Analysis ML Model"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
